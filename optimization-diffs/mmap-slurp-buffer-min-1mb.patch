--- a/src/lib.rs
+++ b/src/lib.rs
@@ -107,7 +107,7 @@
             if capacity_hint > 0 {
                 map.reserve(capacity_hint);
             }
-            let mut buf: Vec<u8> = Vec::with_capacity(total_on_disk as usize);
+            let mut buf: Vec<u8> = Vec::with_capacity((total_on_disk as usize).max(1 << 20));
             file.read_to_end(&mut buf)?;
 
             let mut offset: usize = 0;
